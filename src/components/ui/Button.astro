---
/**
 * Premium Button Component
 * Variants: primary, secondary, ghost
 * Sizes: sm, md, lg
 */

interface Props {
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  href?: string;
  class?: string;
  [key: string]: any;
}

const { 
  variant = 'primary', 
  size = 'md', 
  href,
  class: className = '',
  ...rest 
} = Astro.props;

const baseStyles = 'inline-flex items-center justify-center font-semibold transition-all duration-[var(--duration-normal)] ease-[var(--ease-in-out)] focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed';

const variants = {
  primary: 'bg-bg-brand text-text-inverse hover:bg-bg-brand-hover focus:ring-border-brand shadow-sm hover:shadow-md',
  secondary: 'bg-bg-surface text-text-primary border-2 border-border-default hover:border-border-brand hover:text-text-brand focus:ring-border-brand shadow-sm hover:shadow-md',
  ghost: 'text-text-brand hover:bg-brand-accent focus:ring-border-brand',
};

const sizes = {
  sm: 'px-4 py-2 text-sm rounded-[var(--radius-md)]',
  md: 'px-6 py-3 text-base rounded-[var(--radius-lg)]',
  lg: 'px-8 py-4 text-lg rounded-[var(--radius-xl)]',
};

const classes = `${baseStyles} ${variants[variant]} ${sizes[size]} ${className}`;

const Element = href ? 'a' : 'button';
---

<Element 
  class={classes}
  href={href}
  {...rest}
>
  <slot />
</Element>
